<% if(typeof errors != 'undefined'){ %>
    <% errors.forEach((err)=>{ %>
        <% console.log("Error inserted" + err.msg) %>
        <div class="alert alert-warning" role="alert">
            <%= err.msg %>
          </div>
    <% }); %>
<% } %>